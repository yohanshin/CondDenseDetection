train:
    type: BEDLAM
    landmark_type: ${landmark_type}
    is_train: true
    condition: ${condition}
    label_path: /large_experiments/3po/data/parsed_data/bedlam_2d_${landmark_type}.pth
    data_cfg: ${data_cfg}
    image_width: ${image_size[0]}
    image_height: ${image_size[1]}
    cond_mask_prob: 0.3
    scale_factor: 0.3
    trans_factor: 0.02
    flip_prob: 0.0
    rotate_prob: 0.6
    trans_prob: 0.5
    rotation_factor: 30
    half_body_prob: 0.0
    use_different_joints_weight: true
    extreme_cropping_prob: 0.2
    ldmk_prompt_prob: 0.0
    ldmk_prompt_ratio: 0.03
    img_aug_prob: 0.9
    soft_nms: false
    num_joints: ${num_joints}
    total_num_joints: ${total_num_joints}

val:
    type: EMDB
    landmark_type: ${landmark_type}
    is_train: false
    condition: ${condition}
    label_path: "/large_experiments/3po/data/parsed_data/emdb1_6fps.pth"
    data_cfg: ${data_cfg}
    image_width: ${image_size[0]}
    image_height: ${image_size[1]}
    scale_factor: 0.
    trans_factor: 0.0
    flip_prob: 0.0
    rotate_prob: 0.0
    trans_prob: 0.0
    rotation_factor: 0
    half_body_prob: 0.0
    use_different_joints_weight: true
    extreme_cropping_prob: 0.0
    img_aug_prob: 0.0
    soft_nms: false
    num_joints: ${num_joints}
    total_num_joints: ${total_num_joints}